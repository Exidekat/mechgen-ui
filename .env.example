# MechGen Environment Variables

# Database Connection (Required)
# Get this from your Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/database

# Alternative database variable (Vercel uses this)
# POSTGRES_URL=postgresql://user:password@host/database

# HuggingFace API Token (Optional)
# For accessing private datasets or higher rate limits
# Get from: https://huggingface.co/settings/tokens
# HUGGINGFACE_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxx

# API Base URL (Optional, for development)
# Used for triggering processing endpoint from datasets API
# VERCEL_URL is automatically set in production
# API_BASE=http://localhost:3000
